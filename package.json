{
  "name": "mvp-projects",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo containing multiple MVP projects",
  "scripts": {
    "build:invoice": "cd invoice-generator && npm install && npm run build",
    "build:lesson": "cd lesson-balance && npm install && npm run build",
    "build:mood-food": "cd mood-food-recommender && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "tree-sitter-javascript": "^0.25.0",
    "tree-sitter-python": "^0.25.0",
    "web-tree-sitter": "^0.26.7"
  }
}
